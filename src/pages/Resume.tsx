import HeroText from "../components/HeroText";

const Home = () => {
    return (
        <section className="mt-44 w-full h-full flex justify-center">
        </section>
    );
};
export default Home;
