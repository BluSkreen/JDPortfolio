import HeroText from '../components/HeroText';

const Home = () => {
    return (
            <section className='w-full grow'>
                <HeroText />
            </section>
           );
}
export default Home;
